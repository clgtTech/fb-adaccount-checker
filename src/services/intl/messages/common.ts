import { defineMessage } from 'react-intl';

export const deleteButton = defineMessage({
  id: 'common.deleteButton',
  defaultMessage: 'Delete',
});

export const saveButton = defineMessage({
  id: 'common.saveButton',
  defaultMessage: 'Save',
});

export const cancelButton = defineMessage({
  id: 'common.cancelButton',
  defaultMessage: 'Cancel',
});

export const editButton = defineMessage({
  id: 'common.editButton',
  defaultMessage: 'Edit',
});

export const closeButton = defineMessage({
  id: 'common.closeButton',
  defaultMessage: 'Close',
});
